import React from 'react';

const Nav = (props) => {
    return (
        <nav>
            <span className="nav-item">
                Message Pusher
            </span>
        </nav>
    )
}

export default Nav;
